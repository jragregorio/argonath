{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "regions": ["sin1"],
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "cd ../.. && npx turbo run build --filter=@warden/web",
  "crons": [
    {
      "path": "/api/cron/device-offline",
      "schedule": "* * * * *"
    }
  ]
}
